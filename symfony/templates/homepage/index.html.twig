{% extends 'base.html.twig' %}

{% block title %}Hello HomepageController!{% endblock %}

{% block body %}
<h1>Liste contacts : </h1>
<table>
<thead>
<tr>
    <th>nom</th>
    <th>prenom</th>
    <th>email</th>
    <th>phone</th>
    <th colspan = "2">options</th>
</tr>
</thead>
<tbody>
<tr>
    {% for contact in contacts %}
        <tr>
            <td>{{contact.firstname}}</td>
            <td>{{contact.lastname}}</td>
            <td>{{contact.email}}</td>
            <td>{{contact.phone}}</td>
            <td><a href={{ path('homepage_delete', { 'id' : contact.id}) }} role="button">delete</a></td>
            <td><a href={{ path('homepage_edit', { 'id' : contact.id}) }} role="button">edit</a></td>
        </tr>
    {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
    {% endfor %}
</tr>
</tbody>
</table>
<a href={{ path('homepage_create') }} role="button">new</a></td>
{% endblock %}
